#include <vector>
#include <string>
using namespace std;

class <%= prob.name %> {
public:
<%= engine = CppEngine.new func, vars
    engine.declare %> {
        return <%= func.type.dumb_cpp %>;
    }
};